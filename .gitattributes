# Handle line endings automatically for text files
* text=auto

# Explicitly set line endings for specific extensions
*.java text eol=lf
*.xml  text eol=lf
*.sh   text eol=lf

# Keep Windows batch files as CRLF so they work on Windows
*.cmd  text eol=crlf
*.bat  text eol=crlf